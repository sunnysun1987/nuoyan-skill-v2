# V2.2.2 Release Validation

Date: 2026-08-05

## Scope

This release fixes four reusable bad cases found during a real respiratory adenovirus project replay:

- long pipelines now persist each completed scenario before the next source runs;
- the `全部证据卡` tab renders all registered cards and labels relevance exclusions;
- delivery artifact existence is separate from business readiness;
- restricted PatentHub collection records an executable retry/browser fallback action;
- transport and parser failures cannot be reported as verified `no_results`.
- concurrent writes to one task directory are not a valid execution pattern; scenario commands that mutate `task.json` must run serially and be verified by rereading the file.

## Local gates

- `python3 -m pytest -q`: 218 passed, 1 skipped;
- `ruff check scripts/`: passed;
- `python3 -m compileall -q scripts`: passed;
- `git diff --check`: passed;
- `doctor --strict --json --output-root /private/tmp/nuoyan-v222-doctor`: `ok=true`;
- package asset parity test: passed.
- user-level install parity: key source, template, skill and version files match the checkout;
- post-install full suite: 218 passed, 1 skipped; installed metadata reports `2.2.2`.

## Real-task replay

Task: `TASK-20260805112226958844`.

The regenerated standard report contains six tabs. The `全部证据卡` panel renders 922 cards: 870 marked `纳入分析` and 52 marked `相关性排除`. The report workflow version is `诺研_skill-code-v2.2.2-2026-08-05`.

`verify-package` reports `delivery_artifacts_ready=true` and `business_ready=false`. The latter remains expected until formal-source access, NMPA manual evidence, reviewed cards, research-integrity claims and conflicts are closed.

## Boundary

This validation proves local source, tests, rendered HTML, package assets and the current user-level runtime path. It does not claim that every external source is currently reachable or that the sample project is business-ready.
